Bootstrap: docker
From: python:3.11

%files
    gym_talos
    config
    pyproject.toml
    poetry.lock

%post
    pip install -U pip
    pip install .

%runscript
    exec python3 -m gym_talos -config /config/config_RL.yaml
